fastapi==0.104.1
uvicorn==0.24.0
firebase-admin==6.2.0
python-dotenv==1.0.0
httpx==0.27.0

# ML training deps (scikit-learn, joblib, numpy) moved to requirements-ml.txt
# — they compile slowly from source on cloud builds and are only needed to
#   train the impression model locally. The app/backend runs without them.
