fastapi==0.104.1
uvicorn==0.24.0
firebase-admin==6.2.0
python-dotenv==1.0.0
httpx==0.27.0

# Optional: required only to TRAIN the impression model (lazy-imported).
# The app runs fine without these — it falls back to the heuristic estimator.
scikit-learn==1.5.2
joblib==1.4.2
numpy==2.1.3
